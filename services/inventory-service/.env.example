PORT=4000
JWT_SECRET=change-this-to-a-long-random-string-in-real-deployments

# MySQL connection (matches the `db` service in docker-compose.yml)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=dev-only-change-me
DB_NAME=smartstock
